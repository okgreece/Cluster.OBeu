language: r
sudo: false
cache: packages

r:
  - oldrel
  - release
  - devel  
before_install:
  - tlmgr install index
github_packages:
  - devtools
r_packages:
 - clValid
 - stats
 - rmarkdown
 - knitr
 - devtools
 - jsonlite
 - reshape
apt_packages:
  - libxml2-dev
sudo: required
warnings_are_errors: false
after_success:
  - Rscript -e 'covr::codecov()'
